# Docs

Architecture, design decisions, and module documentation will live here as the
project grows. Start with `../README.md` for the current project overview.

Policy language direction: when sandbox, filesystem, and network policies are
implemented in the Rust runtime, they will use **Starlark**. See
`../runtime/rust/README.md` for the planned policy model.
