{
  "name": "@dev-agent/executor",
  "version": "0.1.0",
  "private": true,
  "description": "Unified executor abstraction; phase 1 provides a minimal LocalExecutor.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test tests-dist/executor.test.js tests-dist/health-check.test.js tests-dist/mock-binary.test.js tests-dist/protobuf-roundtrip.test.js tests-dist/rust-executor.test.js tests-dist/executor-quotas.test.js tests-dist/executor-cancel.test.js",
    "test:integration": "tsc -p tsconfig.test.json && node --test tests-dist/real-rust-integration.test.js",
    "test:all": "pnpm test && pnpm test:integration",
    "clean": "rm -rf dist tests-dist"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "protobufjs": "^7.0.0"
  }
}
